<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Bazaar</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/products">Home</a>
        </li>
      </ul>
    </div>
    <div class="navbar-nav ml-auto">
      <%if(!currentUser){%>
        <span class="navbar-text">
          <button class="btn btn-outline-primary btn-sm"><a class="nav-link active" href="/signIn">SignIn</a></button>
        </span>
      <%}else{%>
        <a href="#" class="nav-link active text-capitalize">Hello <%=currentUser.username%></a>
        <%if(currentUser.role === "consumer"){%>
            <a href="/user/<%=currentUser._id%>/cart" class="nav-link active"><i class="fas fa-shopping-cart"></i> 
              <sup><span class="badge bg-danger"><%=currentUser.cart.length%></span></sup> 
            </a>
          <%}else{%>
            <li class="nav-item">
              <a class="nav-link" href="/products/new">Add Products</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/products/new">Your Products</a>
            </li>
          <%}%>
        <a href="/logout" class="nav-link active">Logout</a>
      <%}%>
    </div>
  </div>
</nav>
